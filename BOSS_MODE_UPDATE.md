# 老板模式功能更新说明

## 更新内容

根据用户需求，已修改老板模式的行为：**获得焦点时不再自动恢复窗口**。

## 修改详情

### 1. 代码修改

**文件**: `PdfViewerBossModeListener.kt`
- **移除**了 `windowGainedFocus` 事件监听器
- **移除**了 `wasToolWindowVisible` 状态变量
- **保留**了 `windowLostFocus` 事件监听器，用于失去焦点时隐藏窗口

### 2. 行为变化

#### 修改前：
- ❌ 失去焦点 → 隐藏窗口
- ✅ 获得焦点 → 自动恢复窗口

#### 修改后：
- ✅ 失去焦点 → 隐藏窗口
- ❌ 获得焦点 → **不**自动恢复窗口（需要手动点击工具窗口图标）

### 3. 文档更新

已更新以下文档以反映新的行为：
- `BOSS_MODE_FEATURE.md` - 功能说明文档
- `BOSS_MODE_IMPLEMENTATION.md` - 技术实现文档
- `BOSS_MODE_QUICK_START.md` - 快速使用指南
- `PdfViewerBundle.properties` - 国际化文本

## 使用场景

这种修改更适合以下场景：

1. **更强的隐私保护**：切换回来后不会立即显示PDF，避免意外暴露
2. **减少干扰**：不会因为自动恢复而打断当前工作流
3. **更可控**：用户可以决定何时重新显示PDF窗口

## 使用方法

### 隐藏PDF窗口：
- 切换到其他应用程序（自动隐藏）

### 重新显示PDF窗口：
- 点击右侧工具窗口栏的 "PDF Viewer" 图标（手动操作）

## 技术优势

1. **更简单的实现**：移除了状态管理逻辑
2. **更少的代码**：减少了约25行代码
3. **更清晰的意图**：用户完全控制窗口的显示时机
4. **更好的用户体验**：避免了意外的自动恢复

## 编译验证

✅ 代码编译成功，无错误
✅ 所有相关文件语法正确
✅ 符合IntelliJ Platform开发规范

## 总结

老板模式现在只提供"失去焦点时自动隐藏"的功能，不再自动恢复。这为用户提供了更好的控制和隐私保护，同时简化了代码实现。
